# Настройка Telegram Bot для Автоломбарда

## 🎯 Возможности бота

- **Для пользователей:** Просмотр каталога автомобилей через Web App
- **Для администраторов:** Добавление объявлений через пошаговую форму (FSM)
- Поиск автомобилей
- Контактная информация
- Интеграция с Hugo сайтом

## 🚀 Быстрый старт

### 1. Создание бота

1. Найдите в Telegram [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите название бота: `Автоломбард`
4. Введите username бота: `auto_lombard_YOURNAME_bot` (замените YOURNAME)
5. **Сохраните полученный токен!**

### 2. Узнайте свой Telegram ID

1. Найдите в Telegram бота [@userinfobot](https://t.me/userinfobot)
2. Отправьте ему команду `/start`
3. Бот покажет ваш ID (например: `123456789`)
4. **Сохраните этот ID - он понадобится для конфигурации администраторов**

### 3. Настройка проекта

1. Скопируйте пример конфигурации:
```bash
cp config.env.example .env
```

2. Отредактируйте файл `.env`:
```bash
# Ваш токен от BotFather
BOT_TOKEN=1234567890:ABCdefGHIjklMNOpqrsTUVwxyz

# URL вашего сайта (локально или продакшн)
WEBAPP_URL=http://localhost:1313

# Telegram ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321

# Путь к Hugo сайту (относительно папки bot)
HUGO_SITE_PATH=../hugo-site
```

3. Установите зависимости:
```bash
cd bot/
pip install -r requirements.txt
```

### 4. Запуск

#### Локальная разработка

1. Запустите Hugo сервер:
```bash
cd hugo-site
hugo server --port 1313
```

2. В новом терминале запустите бота:
```bash
cd bot/
python main.py
```

#### Продакшн

Используйте Docker:
```bash
docker-compose up -d
```

## 📱 Команды бота

### Для всех пользователей
- `/start` - Главное меню с кнопками
- `/catalog` - Открыть каталог автомобилей
- `/search` - Поиск автомобилей
- `/help` - Справка по командам

### Только для администраторов
- `/add_car` - Добавить новый автомобиль
- Кнопка "➕ Добавить автомобиль" в главном меню
- Кнопка "📊 Статистика" - показывает количество авто в каталоге

## 🔐 Права администратора

### Как настроить администраторов

1. Узнайте Telegram ID нужных пользователей через [@userinfobot](https://t.me/userinfobot)
2. Добавьте ID в файл `.env`:
```bash
ADMIN_IDS=123456789,987654321,111222333
```
3. Перезапустите бота

### Проверка прав

Когда администратор запускает бота `/start`, он видит расширенное меню с дополнительными кнопками:
- ➕ Добавить автомобиль
- 📊 Статистика
- 🚗 Открыть каталог

## ➕ Добавление автомобиля

### Пошаговый процесс

Администратор использует команду `/add_car` или кнопку "➕ Добавить автомобиль", после чего бот запрашивает данные:

1. **Марка** (например: BMW, Toyota, Mercedes-Benz)
2. **Модель** (например: X5, Camry, E-класс)
3. **Год выпуска** (2010-2024)
4. **Цена** в рублях (например: 2500000)
5. **Пробег** в км (например: 85000)
6. **Объем двигателя** в литрах (например: 2.0)
7. **Тип топлива** (выбор из списка: Бензин, Дизель, Гибрид, Электро, Газ)
8. **Коробка передач** (MT, AT, AMT, CVT, Робот)
9. **Тип привода** (Передний, Задний, Полный)
10. **Тип кузова** (Седан, Хэтчбек, Внедорожник и т.д.)
11. **Состояние** (Отличное, Хорошее, Среднее, Требует ремонта)
12. **Цвет** (например: Черный, Белый, Серебристый)
13. **Описание** (свободный текст о комплектации, состоянии и т.д.)
14. **Фотографии** (до 10 шт, загружаются по одной)
15. **Подтверждение** - бот показывает предпросмотр

### Отмена добавления

На любом этапе можно нажать кнопку "❌ Отменить" для прерывания процесса.

### Что происходит после создания

1. Бот автоматически создает `.md` файл в `hugo-site/content/cars/`
2. Фотографии сохраняются в `hugo-site/static/images/cars/`
3. Объявление появляется на сайте после:
   - Перезапуска Hugo сервера (локально)
   - Автоматического деплоя через GitHub Actions (продакшн)

### Формат данных

- **Год:** целое число от 1990 до текущего года
- **Цена:** целое число в рублях (можно с пробелами: 2 500 000)
- **Пробег:** целое число в км
- **Объем двигателя:** число с точкой (2.0) или запятой (2,0)
- **Описание:** любой текст (можно использовать переносы строк)

## 🌐 Для продакшена

### Развертывание веб-сайта

Для работы Web App в Telegram нужен **HTTPS**. Варианты:

#### 1. GitHub Pages (рекомендуется)

Уже настроено! Просто:
```bash
git add .
git commit -m "Добавлен новый автомобиль"
git push origin main
```

Сайт автоматически обновится на `https://yourusername.github.io/tg_bot_auto_lombard`

#### 2. Vercel / Netlify

Подключите репозиторий к платформе, выберите Hugo в настройках.

#### 3. Ngrok (для тестирования)

```bash
npm install -g ngrok
cd hugo-site && hugo server --port 1313 &
ngrok http 1313
# Скопируйте HTTPS URL в .env
```

### Обновление Web App URL

После получения HTTPS URL:

1. Обновите `.env`:
```bash
WEBAPP_URL=https://yourusername.github.io/tg_bot_auto_lombard
```

2. Обновите кнопку меню в @BotFather:
   - Отправьте `/mybots`
   - Выберите своего бота
   - `Bot Settings` → `Menu Button` → `Configure menu button`
   - Введите URL: `https://yourusername.github.io/tg_bot_auto_lombard/cars/`

3. Перезапустите бота

## 🔧 Настройка бота в BotFather

### Описание бота

```
/setdescription
🚗 Автоломбард - каталог качественных автомобилей с пробегом

📱 Удобный поиск по марке, модели, цене
💰 Выгодные предложения
📞 Связь с менеджерами
🔍 Подробные характеристики

Нажмите "Меню" для просмотра каталога!
```

### О боте

```
/setabouttext
Автоломбард - ваш надежный помощник в выборе автомобиля с пробегом. Качественные автомобили, честные цены, полная проверка документов.
```

### Команды

```
/setcommands
start - Главное меню
catalog - Каталог автомобилей
search - Поиск автомобилей
add_car - Добавить автомобиль (только для админов)
help - Справка
```

### Фото для бота

Добавьте фото (512x512 px) через:
```
/setuserpic
```

## 🐛 Возможные проблемы

### Ошибка "Web App not supported"
- Убедитесь, что используете **HTTPS** URL
- Проверьте, что сайт доступен по указанному URL
- HTTP работает только на localhost

### Бот не отвечает
- Проверьте правильность токена в `.env`
- Убедитесь, что бот запущен (`python main.py`)
- Проверьте логи в терминале

### "У вас нет доступа к этой команде"
- Проверьте, что ваш Telegram ID добавлен в `ADMIN_IDS`
- Убедитесь, что ID указан правильно (только цифры, без пробелов)
- Перезапустите бота после изменения `.env`

### Web App не открывается
- Проверьте, что Hugo сервер запущен
- Убедитесь, что URL в `.env` совпадает с реальным
- Для продакшена нужен HTTPS

### Фотографии не загружаются
- Проверьте права доступа к папке `hugo-site/static/images/cars/`
- Убедитесь, что путь `HUGO_SITE_PATH` в `.env` указан правильно
- Проверьте, что папки существуют

### Объявление не появляется на сайте
- Локально: перезапустите Hugo сервер
- Продакшн: сделайте commit и push в Git
- Проверьте, что файл создался в `hugo-site/content/cars/`

## 📊 Мониторинг

### Логи бота

Бот выводит подробные логи:
```
INFO - 🚗 Запуск Telegram бота Автоломбарда
INFO - Web App URL: https://...
INFO - ✅ Настроено администраторов: 2
```

### Проверка созданных объявлений

```bash
# Посмотреть список объявлений
ls hugo-site/content/cars/

# Посмотреть последнее объявление
ls -lt hugo-site/content/cars/ | head -2
```

## 🔄 Рабочий процесс

### Типичный сценарий использования

1. Администратор получает информацию о новом автомобиле
2. Открывает бота в Telegram
3. Использует `/add_car` или кнопку
4. Заполняет форму (5-10 минут)
5. Загружает фотографии
6. Проверяет предпросмотр
7. Подтверждает создание
8. Объявление автоматически появляется на сайте!

### Автоматизация

GitHub Actions автоматически:
- Собирает Hugo сайт при каждом push
- Деплоит на GitHub Pages
- Обновляет каталог автомобилей

Администраторам не нужно:
- Знать Hugo или Markdown
- Работать с Git напрямую
- Редактировать файлы вручную
- Загружать фото на сервер

Всё делается через Telegram! 🎉

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи бота в терминале
2. Убедитесь, что все переменные в `.env` заданы правильно
3. Проверьте права доступа к файлам и папкам
4. Убедитесь, что используете актуальные версии библиотек

---

**Удачи в использовании! 🚗**
